[project]
name = "slaythespirellm"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "langchain-openai>=1.4.2",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
